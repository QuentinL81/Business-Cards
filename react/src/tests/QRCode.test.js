import React from 'react';
import { render, screen } from '@testing-library/react';
import QRCodeGenerator from '../blocs/QRCode';

describe('Tests QRCode bloc', () => {
  test('should render correctly with default color', () => {
  });

});